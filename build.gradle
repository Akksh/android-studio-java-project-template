plugins {
    id 'java'
}

sourceCompatibility = 1.8
version = '0.1'

repositories {
    mavenCentral()
}

dependencies {
    // ##testing##

    // Junit
    testImplementation group: 'junit', name: 'junit', version: '4.11'
}
